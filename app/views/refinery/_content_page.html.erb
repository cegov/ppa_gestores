<%= 
	# render_content_page(@page, {
      # :hide_sections => local_assigns[:hide_sections],
      # :can_use_fallback => !local_assigns[:show_empty_sections] && !local_assigns[:remove_automatic_sections]
    # }) 
%>
<div class="not-so-fluid">
	<div class="container-fluid">
		<div class="row-fluid">	
			 
			<div class="span3 sidebar-nav">
				<% 
					# render '/refinery/show_newsarticle', :newsarticles => @newsarticles 
				%>
				<div id="sidebar_menu_geral">
					<div class="colored-box">		
						<h4>Geral</h4>											
					</div>
					<ul>
						<li><%= # link_to @sobre.title.downcase, @sobre.url if !@sobre.nil? 
						%></li>
						<li><%= # link_to "Notícias".downcase, newsarticles_newsarticles_url 
						%></li>
						<li><%= # link_to "Eventos".downcase, events_newsarticles_newsarticles_url 
						%></li>
						<li><a href="/sobre">sobre</a></li>
						<li><a href="/newsarticles">notícias</a></li>
						<li><a href="/courses">cursos</a></li>
					</ul>	
				</div>
				<% 
					# raw @page.content_for(:side_body) # ficará sempre igual. por isso está comentado 
				%>				
			</div>
						
			<div class="span9">				
				<%=raw @page.content_for(:body) %>	
				<%= yield :body %>
			</div>
		</div>
	</div>
</div>
<%= render '/refinery/draft_page_message' unless @page.try(:live?) -%>
